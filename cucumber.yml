default:
  parallel: 2
  format:
    - html:test/cucumber-report.html
    - progress-bar
  # import:   #support code
  #   - 'test/support/**/*.js'
  paths:   # features
    - 'test/features/**/*.feature'
  worldParameters:
    browser: chrome
    base_url: https://localhost:5000
    keepBrowserOpen: True